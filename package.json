{
  "name": "trm-docs",
  "private": true,
  "scripts": {
    "setup": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "serve": ".venv/bin/zensical serve",
    "build": ".venv/bin/zensical build --clean"
  }
}
